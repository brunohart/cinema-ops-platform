# Copy to .env and fill in. Never commit .env.
TMDB_API_KEY=
# DB=postgresql://cinema:cinema@localhost:5432/cinema_ops
# Redpanda (docker-compose service advertises localhost:19092)
# KAFKA_BOOTSTRAP=localhost:19092
# KAFKA_TOPIC=ticketing.bookings
# KAFKA_GROUP_ID=cinema-ops-events
# Agent tools (VDE-44) — bearer token required; role DSN preferred over cinema superuser
# AGENT_TOOL_TOKEN=dev-token-change-me
# AGENT_DATABASE_URL=postgresql://agent_readonly:change-me-at-provision@localhost:5432/cinema_ops
# AGENT_TOOLS_PORT=8787
