# Copy to .env and fill in. Never commit .env.
TMDB_API_KEY=
# DB=postgresql://user:pass@localhost:5432/cinema_ops
# Stream transport: unset → file log under STREAM_ROOT (default ./var/stream).
# Set to use Redpanda from docker-compose (ADR-007):
# KAFKA_BOOTSTRAP=localhost:19092
# STREAM_ROOT=./var/stream
# STREAM_GROUP=cinema-ops
